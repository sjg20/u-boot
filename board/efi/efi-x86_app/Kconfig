if EFI_APP

config SYS_BOARD
	default "efi-x86_app"

config SYS_VENDOR
	default "efi"

config SYS_SOC
	default "efi"

config SYS_CONFIG_NAME
	default "efi-x86_app" if !TARGET_CHROMEOS_EFI_APP32

config BOARD_SPECIFIC_OPTIONS # dummy
	def_bool y
	imply VIDEO_EFI

endif

if TARGET_CHROMEOS_EFI_APP32

config SYS_CONFIG_NAME
	default "chromeos_efi-x86_app"

endif
